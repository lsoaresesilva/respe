/* import { ComponentFixture, TestBed } from '@angular/core/testing';

import { CardErrosProgramacaoDoughnutComponent } from './grafico-pizza-erros-conceitos.component';

describe('CardErrosProgramacaoDoughnutComponent', () => {
  let component: CardErrosProgramacaoDoughnutComponent;
  let fixture: ComponentFixture<CardErrosProgramacaoDoughnutComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ CardErrosProgramacaoDoughnutComponent ]
    })
    .compileComponents();
  });

  beforeEach(() => {
    fixture = TestBed.createComponent(CardErrosProgramacaoDoughnutComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
 */
